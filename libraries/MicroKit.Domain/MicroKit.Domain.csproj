<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <Description>DDD primitives for .NET 10 — AggregateRoot, Entity, ValueObject, DomainEvent, Specifications, Repository abstractions.</Description>
    <PackageTags>microkit;ddd;domain;aggregateroot;valueobject;dotnet</PackageTags>
  </PropertyGroup>

</Project>
